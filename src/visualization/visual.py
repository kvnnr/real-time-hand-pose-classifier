

class Visual:
    """
    Renders hand tracking and prediction on video stream.

    Input:
        frame (image)
        landmarks
        predicted_gesture

    Process:
        Draws hand skeleton
        Draws landmarks
        Displays gesture label on screen

    Output:
        annotated_frame (image)
    """
    pass