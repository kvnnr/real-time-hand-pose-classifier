

class PoseCollector:
    """
    Collects labeled gesture samples for dataset creation.

    Input:
        normalized_landmarks
        label (gesture name)

    Process:
        Stores samples in memory or temporary buffer
        Associates landmarks with label

    Output:
        stored sample (landmark + label pair)
    """
    pass