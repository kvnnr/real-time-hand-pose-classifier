


class PoseDataset:
    """
    Persists collected gesture data into dataset format.

    Input:
        collected_samples (landmarks + labels)

    Process:
        Converts data into tabular format
        Saves dataset as CSV file

    Output:
        CSV file saved to disk
    """
    pass