

class TrainModel:
    """
    Trains ML model for gesture classification.

    Input:
        dataset CSV (features + labels)

    Process:
        Splits data into train/test
        Learns mapping between landmarks and gesture labels

    Output:
        trained_model (saved file)
    """
    pass